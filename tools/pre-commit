#!/bin/sh
# SPRINT DOOM harness gate: syntax + game-balance invariants on every commit.
set -e
cd "$(git rev-parse --show-toplevel)"
for f in js/*.js; do
  node --input-type=module --check < "$f" || { echo "SYNTAX GATE: $f fails to parse"; exit 1; }
done
node tools/balance-check.mjs
