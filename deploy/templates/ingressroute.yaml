apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: {{ .Values.name }}
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`{{ include "app.hostname" . }}`)
      kind: Rule
      services:
        - name: {{ .Values.name }}
          port: 80
  tls: {}
